{
  "framework": "nextjs",
  "buildCommand": "astro build && next build",
  "outputDirectory": ".next",
  "rewrites": [
    {
      "source": "/generator/:path*",
      "destination": "/generator"
    }
  ]
}
